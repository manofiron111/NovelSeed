# 安全策略

## 报告漏洞

如果你发现安全漏洞，请不要通过公开 Issue 报告。

请发送邮件至项目维护者（通过 GitHub 私信联系），我们会尽快处理。

## 本项目的安全边界

NovelSeed 是一个 Agent Skills 仓库——它不执行代码，不连接外部服务，不存储用户数据。

安全风险仅限于：
- 生成的文本内容（不包含可执行代码）
- 模板中的示例（均为通用构造，不含真实信息）
